<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ming.car.mapper.CarMapper">
    <select id="getBatteryTypeByCarId" resultType="com.ming.car.entity.dto.Car">
        select * from car where car_id = #{carId}
    </select>
</mapper>
